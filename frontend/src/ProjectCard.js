import styled from "styled-components";
import { Icon, Avatar, Card } from "antd";

const ProjectCard = styled(Card)`
  width: 50%;
  margin: 0 auto;
  margin-bottom: 20px;
`;

export { ProjectCard };
